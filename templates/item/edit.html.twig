{% extends 'base.html.twig' %}

{% block title %}Item : create
{% endblock %}
{% block content %}
    <h2>New item</h2>
    {{form_start(itemForm)}}
    {{form_widget(itemForm)}}
    <button type="submit" class="btn btn-primary">Create</button>
    {{form_end(itemForm)}}
{% endblock %}
